# Developer Agent Memory Index

- [SFCORE_Stub.Builder List<X> param false positive](feedback_sfcore_stub_list_param_false_positive.md) — use a direct override subclass, not SFCORE_Stub.Builder, for any method with a List<X>/Id parameter
- [Apex has no isPolymorphicForeignKey()](reference_apex_no_ispolymorphicforeignkey.md) — detect polymorphic lookups via field.getReferenceTo().size() > 1 instead
- [SDD brief scope vs compile safety](feedback_sdd_brief_scope_vs_compile_safety.md) — trace every consumer (Apex, Flow, LWC/Aura) of a method before changing it, then surface findings and options to the controller instead of picking an outcome yourself
- [SDD verification: deploy before test](feedback_sdd_verification_deploy_before_test.md) — deploy new/modified classes before running Apex tests against them; report pass-rate and coverage as separate gates
- [Chunk-overflow branch coverage](feedback_chunk_overflow_branch_coverage.md) — override the query seam to return chunkSize+1 rows and override the DML seam to avoid CPU/heap cost
- [Apex System.debug(WARN) logging idiom](reference_apex_debug_warn_logging_idiom.md) — this codebase's only "log and skip" pattern for per-item error isolation; no custom Logger class exists
- [SFCORE_RepoFactory.Facade field-list cache](reference_sfcore_repo_facade_field_list_cache.md) — two methods querying the same SObjectType in one transaction must share one query method or a later call silently loses fields
- [CMDT picklist must be restricted](reference_cmdt_picklist_must_be_restricted.md) — non-strict picklists are rejected on __mdt objects; always set restricted=true even if the brief says otherwise
- [CMDT-driven discovery test seeding](feedback_cmdt_driven_discovery_test_seeding.md) — when a CMDT drives which objects exist (not just their classification), a test must seed one row per object it needs recognized
- [Polymorphic lookup field allowlist](reference_polymorphic_lookup_allowlist.md) — when branching on a polymorphic lookup's type, allowlist the types the logic applies to explicitly; never bucket everything else into one branch
- [SFCORE_RepoFactoryMock filter blindness](reference_sfcore_repofactorymock_filter_blindness.md) — mocked .get() ignores query filters and returns every seeded row for the SObjectType; give election-test winners a clearly larger row count
- [Scope git commit with pathspec](feedback_scope_git_commit_with_pathspec.md) — always `git commit -m ... -- <files>`; a bare commit after `git add` commits the whole index, including other sessions' pre-staged files
- [Dynamic DML: bare row, not queried row](reference_dynamic_dml_bare_row_from_queried_row.md) — USER_MODE FLS checks every populated field on the DML row, not just the changed one; use SObjectType.newSObject(id) for dynamic types
- [Apex reserved word "group"](reference_apex_reserved_word_group.md) — never name a local var/param `group`; fails only at deploy time with "Identifier name is reserved"
- [Same-transaction DML visibility test double](reference_same_transaction_dml_visibility_test_double.md) — snapshot committed state per doUpdateRows call to prove a two-phase DML split fixes an external-Flow ordering bug
- [Approval before changing any org](feedback_explicit_target_org.md) — query, retrieve, describe and running an existing test are fine anywhere; anything that writes needs explicit approval, and which environments accept a direct deploy is the team's promotion policy
- [ESLint unavailable locally](reference_eslint_unavailable_locally.md) — running eslint directly fails for more than one reason, so read the error; `sf code-analyzer run` needs no Java for eslint, and a project-accurate result means the bundled rule layers off plus a positive control before believing a zero
- [Code analyzer working recipe](reference_code_analyzer_working_recipe.md) — when `gulp codeAnalyzer` aborts, Java is not the reason: it supplies its own JDK, and the cause is a scanner version conflict in the shared build library
- [Location SObject vs System.Location](reference_location_sobject_vs_system_location.md) — always write Schema.Location; bare Location resolves to the geolocation type and breaks generic field-setting helpers too
- [@AuraEnabled Apex-type param arrives all-null](reference_auraenabled_apex_type_param_arrives_null.md) — send the payload as a String and JSON.deserialize it; never retype the wrapper's fields
- [Apex tests cannot run without a deploy](reference_apex_tests_cannot_run_without_deploy.md) — check-only deploys accept RunSpecifiedTests but run zero tests; report compile-validated, not executed
- [Experience Cloud icon colour reset](reference_experience_cloud_icon_color_reset.md) — an Experience Cloud site's injected reset kills both icon colour tokens and variants; set `color` on the icon too
- [Surface mockup conflicts, don't decide](feedback_surface_mockup_conflicts_dont_decide.md) — build the binding instruction, flag the mockup conflict with values + switch-cost; confirmed right call
- [lightning-input toggle message position](reference_lightning_input_toggle_message_position.md) — the Yes/No word always stacks under the switch; blank both messages and render it yourself
- [Jest mocks keyed by args, not call order](reference_jest_mock_keyed_by_args_not_call_order.md) — moving when an existing Apex call fires desyncs positional mockResolvedValueOnce queues; key by actual arguments instead
- [aria-* boolean binding renders as string](reference_aria_boolean_binding_renders_string.md) — bind a boolean getter straight to aria-disabled/aria-expanded; LWC already stringifies it, no manual 'true'/'false' getter needed
- [prettier --write reformats whole touched file](reference_prettier_write_reformats_whole_touched_file.md) — check first; a non-prettier-clean file explodes into a whitespace-migration diff, hand-format new lines instead
- [LWC for:each key convention](reference_lwc_foreach_key_convention.md) — key on the inner element via item.field, not for:key on <template>; decorate plain string arrays into {key, name} objects
- [Jest label mock convention](reference_jest_label_mock_convention.md) — @salesforce/label imports are mocked per-label with their own name string, not real text; sfdx-lwc-jest does not auto-resolve labels-meta.xml
- [Array.find() + toBeNull() is vacuous](reference_find_plus_tobenull_vacuous.md) — .find() returns undefined not null on a miss; use toBeDefined()/toBeUndefined() for .find() results, reserve toBeNull() for querySelector
- [Jest scans nested worktrees](reference_jest_scans_nested_worktrees.md) — relative test-path patterns match the same file inside every `.claude/worktrees/*` checkout too; pass an absolute path to scope a run
- [CSS var dead when flex disabled](reference_css_var_dead_when_flex_disabled.md) — a var() reference isn't proof a token is load-bearing; check for a commented-out display:flex on the same rule first
- [StaticResource description 255-char limit](reference_staticresource_description_255_limit.md) — count the field before deploying a rewritten description; a full-sentence-per-file description overruns it
- [Shared sandbox deploy conflict](reference_shared_sandbox_deploy_conflict.md) — a conflict can mean another worktree deployed first rather than real drift; verify with a read-only retrieve
- [Verify brief claims against file state](feedback_verify_brief_claims_against_file_state.md) — a brief can describe a comment/code state that never existed in the file; read it first, flag the mismatch instead of silently "correcting"
- [SLDS position utility vs component CSS](reference_slds_position_utility_vs_component_css.md) — delete a conflicting slds-is-relative/absolute class rather than trust cascade order
- [HEAD moves mid-task — verify against the index](reference_head_moves_midtask_use_index_baseline.md) — prove a scoped change with `git show :<path>`, never a HEAD diff; `MM` in git status is the tell
